import { View } from 'react-native';

// This screen is hidden from tab bar (href: null in _layout.tsx).
// History functionality has been merged into index.tsx.
export default function HistoryScreen() {
  return <View />;
}
